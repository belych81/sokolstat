{% extends 'eurocup/newMatch.html.twig' %}

{% block title %}
    Добавить сезон
{% endblock %}

{% block description %}
{% endblock %}

{% block seasons %}
{% endblock %}

{% block content %}

<form action="{{ path('sbornie_createSeason',
  {'turnir': app.request.get('turnir'), 'season': app.request.get('season')}) }}"
      method="post" >
   <table>
        <tbody>
            <tr>
                <th>{{ form_label(form.stadia) }}</th>
                <td>
                    {{ form_errors(form.stadia) }}
                    {{ form_widget(form.stadia) }}
                </td>
            </tr>

            <tr>
                <th>{{ form_label(form.country) }}</th>
                <td>
                    {{ form_errors(form.country) }}
                    {{ form_widget(form.country) }}
                </td>
            </tr>
        </tbody>
   </table>
    <p>
        {{ form_row(form._token) }}
        <button type="submit" class="spiski">Добавить</button>

    </p>
</form>
{% endblock %}
